#!/usr/bin/env groovy

pipeline {

    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                echo 'Building test project...'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
    }
}
